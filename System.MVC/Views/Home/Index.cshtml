@{
    ViewData["Title"] = "Home Page";
}

@if (User.IsInRole(Roles.SuperAdmin) || User.IsInRole(Roles.Admin) || User.IsInRole(Roles.Manager))
{
        <partial name="_HomePartial" />

}
else
{
    <partial name="_UserHomePartial" />
}

